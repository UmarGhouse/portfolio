# TODO: TEST DEPLOY

setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
  config:
    RAILS_ENV: production
    # FOO: bar
release:
  command:
    - rails db:migrates
  # image: worker
run:
  web: bundle exec rails s -b 0.0.0.0